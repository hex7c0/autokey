"use strict";

function autokey(password, lodash) {
    return lodash ? require(min + "lodash/index.js")(password) : require(min + "normal/index.js")(password);
}

var min = __dirname + "/min/lib/";

module.exports = autokey;
